# 콘텐츠 계획 (자동 발행용)

이 파일은 매일 자동으로 글을 쓰는 세션이 참고하는 계획서다. 사이트에는 노출되지 않는다(`_config.yml`의 `exclude`). 블로그는 공개 상태이며 애드센스 심사를 준비 중이다(광고 스크립트와 `ads.txt`가 이미 들어가 있음).

## 글쓰기 규칙

- **분량은 글마다 다르게.** 기본은 1,500~3,000자지만, 한 가지 아이디어만 다루는 800~1,200자짜리 짧은 글도 주 1편 정도 섞는다. 매번 비슷한 길이가 되지 않게 한다.
- **문체**: 존댓말. 담백하게. 과장·감탄사·이모지를 쓰지 않는다.
- **구성은 고정하지 않는다.** 아래 형식을 섞어 쓴다. 직전 2~3편과 같은 형식이 반복되지 않게 한다.
  - 개념 설명형: 개념 → 예시/코드 → 주의점. "정리" 섹션은 글이 길 때만 넣고, 짧은 글은 마지막 문단으로 자연스럽게 끝낸다.
  - 실험형: 질문 하나를 던지고 코드로 확인해 본 과정과 결과. 결과가 예상과 달랐으면 그 점을 솔직하게 쓴다.
  - 삽질 기록형: "이걸 하려다 막혔다 → 원인 → 해결 또는 미해결". 짧아도 된다.
  - 생각 정리형: 코드 없이 읽은 논문·책·자료에 대한 소감과 의문. 카테고리 `블로그` 또는 `퀀트투자`.
- **"다음 글" 예고는 시리즈 번호가 붙은 글에서만**, 그것도 자연스러울 때만 쓴다. 매번 넣지 않는다.
- **소제목 개수도 고정하지 않는다.** 2개짜리 글도, 7개짜리 글도 있어야 한다.
- **코드**: 넣을 때는 복사해서 바로 돌아가야 한다. pandas / FinanceDataReader / matplotlib 위주. 모든 글에 코드가 있어야 하는 것은 아니다. 수식은 LaTeX 대신 코드블록이나 표로 표현한다(테마가 수식을 렌더링하지 않음).
- **실제 결과를 우선한다.** `scripts/` 에 실행 가능한 스크립트가 있고 결과가 확보되어 있으면, 코드만 보여주고 "직접 돌려보세요"로 끝내지 말고 실제 수치와 차트를 글에 넣는다.
- **면책 문구**: 투자 관련 글은 본문 맨 끝에 아래를 붙인다.

  ```
  ---

  *이 글은 학습과 정보 공유 목적으로 작성되었으며, 특정 종목이나 상품에 대한 투자 권유가 아닙니다. 투자의 판단과 책임은 투자자 본인에게 있습니다.*
  ```

- **연속성**: 이전 글에서 이미 설명한 개념은 다시 처음부터 설명하지 않고 링크하듯 언급한다. 새 개념이 나오면 반드시 그 자리에서 정의하고 넘어간다.

## 금지 사항 (중요 — 반드시 준수)

- **익명 블로그다.** 글쓴이의 실명, 직장, 직업, 소속 기관, 근무 경험, 회사 생활을 일체 언급하지 않는다. "회사에서", "업무상", "실무에서 보니" 같은 표현도 쓰지 않는다. 1인칭은 "저"만 쓰고 배경 정보는 드러내지 않는다.
- 공개되지 않은 정보나 특정 기관 내부의 시각·관행을 아는 듯한 서술을 하지 않는다.
- 공개된 학술 연구, 공개 데이터, 오픈소스 도구만 소재로 삼는다.
- 특정 종목·상품 추천이나 투자 권유로 읽힐 표현을 쓰지 않는다.
- `_includes/head/custom.html`(애드센스 스크립트), 루트의 `ads.txt`와 `sitemap.xml`은 건드리지 않는다. 글만 추가한다.

## 데이터 소스 메모

- 한국 종목 재무·시가총액 스냅샷은 `scripts/krx_snapshot.py YYYYMMDD --market ALL` 로 실제 데이터를 받을 수 있다 (pykrx, 세션 환경변수 `KRX_ID`/`KRX_PW` 와 허용 도메인 `data.krx.co.kr` 설정됨, 2026-09-21 확인). 실전 시리즈 글은 가능하면 이 실제 데이터로 표와 차트를 만든다. 환경변수가 없으면 `--synthetic` 또는 공개 스냅샷으로 대체하고 글에 그 사실을 밝힌다.
- 종목별 월간 수익률은 `scripts/data/krx/returns_<from>_<to>.csv.gz` 21개 구간(2024-12~2026-09, 수정주가·상폐 -100 포함)에 받아 두었다. 스냅샷의 종가는 수정주가가 아니므로 수익률 계산에 쓰지 않는다.
- 자격증명 값은 글·스크립트·로그 어디에도 적지 않는다. pykrx 호출은 `krx_snapshot.quiet()` 로 감싼다 (로그인 시 아이디가 stdout 에 찍힘).

## 파일 형식

`_posts/YYYY-MM-DD-english-slug.md`

```markdown
---
title: "글 제목"
categories:
  - 퀀트투자
tags:
  - 태그1
  - 태그2
---
```

카테고리는 `퀀트투자`, `파이썬`, `블로그` 중에서 고른다.

**시리즈 구분**

- **퀀트 투자 입문** ①~⑳ 은 완결됐다. 더 이상 이 번호를 잇지 않는다. 정리 글 한 편(번호 없음)으로 닫는다.
- 이후 글은 **퀀트 실전** 시리즈다. 제목은 "퀀트 실전 ① — 부제" 형식이고 ①부터 새로 센다. 입문에서 다룬 개념은 링크로 가리키고 다시 설명하지 않는다.
- 시리즈에 속하지 않는 글(도구 소개, 생각 정리)은 번호를 붙이지 않는다.

## 발행 순서

작성 완료된 글은 `[x]`로 표시한다. 다음 발행 대상은 **가장 위에 있는 `[ ]` 항목**이다.

- [x] ① 퀀트 투자란 무엇인가
- [x] ② 수익률 계산의 기초
- [x] ③ 백테스트란 무엇인가
- [x] ④ 모멘텀이란 무엇인가
- [x] ⑤ 첫 백테스트: 절대 모멘텀
- [x] 파이썬으로 주가 데이터 받아오기 (FinanceDataReader)
- [x] ⑥ 거래 비용과 슬리피지 — 백테스트에 현실 넣기
- [x] ⑦ 과최적화(오버피팅) — 가장 유혹적인 함정
- [x] ⑧ 위험 지표 완전 정복 — 변동성, MDD, 샤프비율, 칼마비율
- [x] ⑨ 미래 참조 편향(look-ahead bias)의 여러 얼굴
- [x] ⑩ 생존 편향 — 사라진 종목들이 만드는 착시
- [x] 자산 배분 기초 — 주식과 채권을 섞는 이유
- [x] 리밸런싱은 정말 효과가 있을까 (데이터로 검증)
- [x] 상관관계와 분산투자의 수학
- [x] 밸류 팩터 — 싼 주식은 정말 더 오르나
- [x] 퀄리티 팩터 — 좋은 회사와 좋은 주식은 다르다
- [x] 저변동성 팩터 — 덜 흔들리는 주식의 역설
- [x] 사이즈 팩터 — 소형주 효과는 살아 있나
- [x] 멀티팩터 — 팩터를 섞는 방법들
- [x] 듀얼 모멘텀 — 상대와 절대를 결합하기
- [x] 올웨더 포트폴리오 뜯어보기
- [x] (번호 없음) 퀀트 투자 입문 시리즈 정리 — 20편을 주제별로 묶은 목차 글. 각 글 링크와 한 줄 요약, 시리즈 전체에서 확인한 것 다섯 가지. 데이터 실험 없이 짧게(1,200~2,000자). 카테고리 `퀀트투자`.

**퀀트 실전 시리즈 (①부터 새로 센다)**

- [x] 퀀트 실전 ① — pykrx로 재무 데이터 받아오기
- [x] 퀀트 실전 ② — 종목 스크리닝 시스템 만들기
- [x] 퀀트 실전 ③ — 워크포워드 검증, 과최적화를 잡는 방법
- [x] 퀀트 실전 ④ — 몬테카를로 시뮬레이션으로 전략 검증하기
- [x] 퀀트 실전 ⑤ — 포트폴리오 최적화 (1) 평균-분산 모델
- [ ] 퀀트 실전 ⑥ — 포트폴리오 최적화 (2) 리스크 패리티
- [ ] 퀀트 실전 ⑦ — 켈리 공식과 포지션 사이징
- [ ] 퀀트 실전 ⑧ — 추세추종과 손절 규칙의 실제
- [ ] 퀀트 실전 ⑨ — 세금과 거래 현실, 백테스트와 실전의 마지막 간극
- [ ] 퀀트 실전 ⑩ — 증권사 API로 자동매매 파이프라인 만들기
