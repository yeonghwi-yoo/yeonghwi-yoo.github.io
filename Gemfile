# 로컬에서 미리보기할 때만 필요합니다 (GitHub Pages는 자동 빌드).
# 사용법:
#   bundle install
#   bundle exec jekyll serve
# 이후 http://localhost:4000 에서 확인
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-include-cache", group: :jekyll_plugins
gem "webrick"
